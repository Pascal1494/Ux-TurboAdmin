<form method="post" data-turbo-frame="_top" action="{{ path('app_cat_delete', {'id': cat.id}) }}" data-turbo-confirm="Are you sure you want to delete this item?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat.id) }}">
    <button class="btn">Delete</button>
</form>
